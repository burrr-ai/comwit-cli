# comwit-cli repository instructions

`AGENTS.md` is the canonical repository instruction file. Read and follow it
in full before acting.

Before any pull-request action, follow its `Agent-authored pull requests`
contract. Claude Code must push an exact `claude/*` work branch and use the
central GitHub App publisher; it must never open a PR as `GoodGoodJM` or use a
human-authenticated fallback.
